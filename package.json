{
  "name": "projectkorra-drama-generator",
  "version": "1.0.0",
  "description": "Generates drama around the ProjectKorra community",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "dev": "wrangler dev"
  },
  "author": "Moros",
  "license": "GPLv3",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
 
